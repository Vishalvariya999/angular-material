import { FilterDescriptionsPipe } from './filter-descriptions.pipe';

describe('FilterDescriptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDescriptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
